"use strict";
let userrname = ['admin', 'khan', 'eric', 'main', 'hammad'];
if (userrname.length == 0) {
    console.log(`we need to find some users`);
}
else {
    userrname = [];
    console.log(`we need to find some users because our array is empty`, userrname.length);
}
